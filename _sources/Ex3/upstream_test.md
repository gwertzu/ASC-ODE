# Tests from upstream

The only test supplied with the upstream version has the `mechsystem/mass_spring.ipynb`. This repository has this file, but that contains the experiment with the chain. These two are the same though aside from the added chain in the end of the spring.
